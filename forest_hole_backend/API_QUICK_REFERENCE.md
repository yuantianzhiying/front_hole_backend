# API 快速参考手册

## 🚀 快速开始

### Base URL
```
http://localhost:8000/api/v1
```

### 认证
```http
Authorization: Bearer {token}
```

---

## 📋 接口速查表

### 1️⃣ 认证 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/auth/login` | 用户名密码登录 | ❌ |

---

### 2️⃣ 帖子 (4个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/posts` | 获取帖子列表 | ✅ |
| POST | `/posts` | 发布新帖子 | ✅ |
| GET | `/posts/{id}` | 获取帖子详情 | ✅ |
| DELETE | `/posts/{id}` | 删除帖子 | ✅ |

**查询参数**:
- `page`: 页码 (默认: 1)
- `limit`: 每页数量 (默认: 20)
- `sort`: 排序 (`new`/`hot`)
- `tag`: 标签筛选
- `keyword`: 关键词搜索

---

### 3️⃣ 评论 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/posts/{id}/comments` | 发表评论 | ✅ |

---

### 4️⃣ 表态 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/posts/{id}/reactions` | 表态/取消 | ✅ |

**表态类型**: `like`, `hug`, `popcorn`, `plus1`

---

### 5️⃣ 举报 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/reports` | 提交举报 | ✅ |

**举报理由**: `attack`, `privacy`, `fake`, `spam`, `other`

---

### 6️⃣ 安全检测 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/safety/check-text` | 文本安全检测 | ✅ |

---

### 7️⃣ 漂流瓶 (2个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/bottles` | 扔漂流瓶 | ✅ |
| GET | `/bottles/draw` | 捡漂流瓶 | ✅ |

---

### 8️⃣ 时间胶囊 (2个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/time-capsules` | 创建时间胶囊 | ✅ |
| GET | `/time-capsules/my` | 获取我的胶囊 | ✅ |

---

### 9️⃣ 投票 (3个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/polls` | 创建投票 | ✅ |
| GET | `/polls/{id}` | 获取投票结果 | ✅ |
| POST | `/polls/{id}/vote` | 参与投票 | ✅ |

---

### 🔟 工具 (1个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/utility/daily-info` | 获取每日信息 | ✅ |

---

### 1️⃣1️⃣ 管理员 (4个)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/admin/dashboard` | 仪表盘数据 | 🔑 |
| GET | `/admin/audit/list` | 待审核列表 | 🔑 |
| PUT | `/admin/audit/{id}` | 执行审核 | 🔑 |
| GET | `/admin/reports` | 举报列表 | 🔑 |

🔑 = 需要管理员权限

---

## 📝 常用请求示例

### 用户登录
```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "password123"}'
```

### 获取帖子列表
```bash
curl "http://localhost:8000/api/v1/posts?sort=hot&page=1&limit=10" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 发布帖子
```bash
curl -X POST "http://localhost:8000/api/v1/posts" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"content": "测试帖子", "tag": "study"}'
```

### 发表评论
```bash
curl -X POST "http://localhost:8000/api/v1/posts/1/comments" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"content": "测试评论"}'
```

### 点赞
```bash
curl -X POST "http://localhost:8000/api/v1/posts/1/reactions" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"type": "like"}'
```

---

## 🎯 状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 参数错误 |
| 401 | 未认证 |
| 403 | 权限不足 |
| 404 | 不存在 |
| 422 | 验证失败 |
| 500 | 服务器错误 |

---

## 🏷️ 枚举值

### 帖子标签
```
crush, rant, help, confession, study, life, other
```

### 表态类型
```
like, hug, popcorn, plus1
```

### 举报理由
```
attack, privacy, fake, spam, other
```

### 帖子状态
```
pending, active, hidden, deleted
```

---

## 💡 提示

1. **Token 有效期**: 7天
2. **举报阈值**: 5次自动隐藏
3. **匿名昵称**: 自动生成，如"焦虑的高三党"
4. **敏感词**: 自动过滤为 `***`
5. **SOS检测**: 识别自杀/抑郁关键词

---

## 📚 完整文档

查看完整文档: `API_DOCUMENTATION.md`

---

**总计**: 21 个 API 接口 ✅
